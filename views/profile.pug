extends template.pug

block content
  p.mt-4 Hello, #{name}! Here are your liked recipes:
  |
  if recipes
    include includes/recipemodal
    .row
      each recipe in recipes
        include includes/recipecard
  else 
    p You haven't liked any recipes yet! Why not 
      a(href="/") search for some?
